#include "sort.h"

/**
* quick_sort - quick_sort
* @array: array of int
* @size: size
* Return: void
*/
void quick_sort(int *array, size_t size)
{
	if (list == NULL || *list == NULL || (*list)->next == NULL)
		return;
}
